Para ejecutar mediante docker-compose introducir los siguientes comandos en la terminas:

Primero debe construis la imagen:

docker build -t "nombre"

Para iniciar el container:

$ docker-compose up -d

Para parar el container:

$ docker-compose stop
